import { component, html } from 'https://cdn.skypack.dev/haunted';

/** @type {CustomElementConstructor} */
export const FindDuplicatesButton = component(() => {
	return html`<button>Find Duplicates</button>`;
});
